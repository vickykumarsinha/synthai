import express from "express";
//import { createPaper, getUserPapers } from "../controllers/researchPaperController.js";
const router = express.Router();
//import { protect } from "../middleware/authMiddleware.js";

//router.post("/", createPaper); // Create new paper
//router.get('/:id/getpapers', getUserPapers); // Get all papers of logged-in user

export default router;
